import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds & Surfaces
  static const Color background = Color(0xFF222831);
  static const Color surface1 = Color(0xFF393E46);
  static const Color surface2 = Color(0xFF2B303A); // Slightly darker surface

  // Accents
  static const Color primary = Color(0xFF00ADB5);
  static const Color primaryLight = Color(0xFF33BDC4);
  static const Color positive = Color(0xFF00E396);
  static const Color negative = Color(0xFFFF4D4D);
  static const Color warning = Color(0xFFFFB020);

  // Typography
  static const Color textPrimary = Color(0xFFEEEEEE);
  static const Color textSecondary = Color(0xFFB0B3B8); // A softer grey for secondary text

  // Dividers & Borders
  static const Color border = Color(0xFF393E46);
}
